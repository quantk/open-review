{
  "name": "open-review",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Local MR-like review loop for opencode agent changes.",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "index.js",
    "dist",
    "src",
    "commands",
    "README.md"
  ],
  "scripts": {
    "build": "esbuild src/runtime.ts --bundle --platform=node --format=esm --target=node20 --packages=external --outfile=dist/index.js",
    "check": "node --check src/runtime.ts",
    "smoke": "node src/runtime.ts --local-review-server",
    "test": "node --test tests/*.test.ts"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.14.30"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/quantk/open-review.git"
  },
  "keywords": [
    "opencode",
    "plugin",
    "review",
    "diff",
    "local-review"
  ],
  "license": "MIT"
}
